# Common Development Tools Project Rules

> **운영 수칙 SSOT**: 이 파일(`CLAUDE.md`)이 `tools` 저장소의 프로젝트 운영 수칙이다.
> 상위 workspace 수칙(`/Users/msjang/Documents/project/AGENTS.md`)과 공유 에이전트 수칙(`/Users/msjang/.agent/AGENTS.md`)을 함께 따른다.

## 프로젝트 정의

`tools`는 여러 프로젝트에서 공통으로 사용하는 개발, 빌드, 문서화 유틸리티 모음이다.

- `md2pdf/`: Markdown을 HTML/PDF로 변환하는 Go 기반 통합 도구
- `_archive/`: 대체되었거나 사용 중단된 도구 보관 영역
- 루트의 `*.sh`, `*.bat`: 호환 래퍼 또는 단일 목적 유틸리티

## 작업 원칙

- 변경 전 대상 하위 도구를 먼저 식별하고, 관련 README/소스/스크립트를 읽는다.
- 요청 범위 밖의 도구와 아카이브 파일은 건드리지 않는다.
- 생성물, 바이너리, 임시 산출물은 필요한 경우에만 갱신하고 이유를 보고한다.
- 기존 사용자 변경은 되돌리지 않는다.

## 검증 원칙

- Go 도구는 해당 하위 디렉토리에서 `go test ./...`, `go vet ./...`, `go build`를 우선 확인한다.
- 스크립트는 실행 전에 내용을 읽고, 가능하면 `bash -n` 같은 비파괴 문법 검사를 먼저 수행한다.
- NAS, 배포, 외부 서비스, 자격증명, 방화벽을 건드리는 작업은 명시 요청 없이 실행하지 않는다.

## 커밋 원칙

- 커밋 메시지는 **한국어**로 작성한다.
- 형식은 Conventional Commit을 유지하되 제목과 본문은 한국어를 기본으로 한다.
  - 예: `fix(md2pdf): PDF 생성 레이아웃 안정화`
  - 예: `docs: 도구 저장소 수칙 추가`
- 한 커밋에는 하나의 논리 단위만 담는다.
- 커밋 전 `git status --short`, `git diff --stat`로 범위를 확인한다.
- 사용자가 명시적으로 요청한 경우에만 push한다.

## 문서화 원칙

- 동작 방식이 바뀌면 해당 도구의 README 또는 관련 문서를 함께 갱신한다.
- 단순 실험/폐기 작업은 장기 문서에 남기지 않는다.
- 사용자에게 보고할 때는 실행한 명령, 검증 결과, 남은 위험을 짧게 정리한다.
