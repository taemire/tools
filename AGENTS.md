# Common Development Tools Agent Bootstrap

`CLAUDE.md` is the single source of truth for this repository's operating rules.

Before doing any work, every agent must:

1. Read `CLAUDE.md`.
2. Follow `CLAUDE.md` exactly.
3. Treat this file as a bootstrap pointer only.

Do not duplicate project rules here. When rules change, update `CLAUDE.md`.
